##注意事项
1.打包,通过winscp上传文件,不要删除上一次发布的文件,等到下一次在发布的时候删除  
2.确认一下是否有修改表结构  

##常用命令
1.查看72村运行的进程 `ps -ef | grep 72cun`  
2.查看输出日志 `tail -f /72cun/temp.log`  

##部署步骤
1.使用maven打包并上传至服务器   
2.运行脚本命令 `/bin/bash /72cun.sh`  

